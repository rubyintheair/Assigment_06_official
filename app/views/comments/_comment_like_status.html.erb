<%= link_to toggle_like_path(comment_id: comment.id), class: "level-item js-comment-like", 
            method: "post", remote: "true" do %>
  <% current_user.liking?(comment) ? "Unlike" : "Like" %>
<% end %>
<span><%= comment.post.updated_at.to_s(:short) %></span>