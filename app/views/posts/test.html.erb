<div class="tile is-ancestor">
  <div class="tile is-4 is-vertical is-parent">
    <div class="tile is-child box">
      This is tile is-4
    </div>
    <div class="tile is-child box">
      This is tile with nothing else but still in the same parent
    </div>
    <div class="tile is-child box">
      This is tile with nothing else but still in the same parent
    </div>
    <div class="tile is-child box">
      This is tile with nothing else but still in the same parent
    </div>
  </div>
  <div class="tile is-parent is-vertical">
    <div class="tile is-child box is-20 ">
    This is for testing the 2nd parent - tile is-10
    </div>
    <div class="tile is-child box">
      This is tile with nothing else but still in the same parent
    </div>
    <div class="tile is-child box">
      This is tile with nothing else but still in the same parent
    </div>
  </div>
  <div class="tile is-parent is-vertical">
    <div class="tile is-child box is-15">
      This is for testing the 2nd parent -  is tile with nothing else but still in the same parent
    </div>
  </div>
</div>


<% @posts.in_groups_of(3, false).each do |group| %>
    <% group.each_with_index do |post, index| %>
      <div class="tile is-ancestor">
        <% if index == 0 %>
        <div class="tile is-parent is-5">
          <div class="tile is-child">
            <%= render 'posts/post', post: post %>
          </div>
        </div>
        <% else index == 1 %>
        <div class="tile is-parent is-5">
          <div class="tile is-child">
            <%= render 'posts/post', post: post %>
          </div>
        </div>
        <% end %>
      </div>
    <% end %>  
<% end %>


<% if post.photo %>
        <div class="content">
          <%= image_tag(post.photo_url) %>
        </div>
      <% end %>
